

%select{ :id => (id or ''), :name => (name or ''), :class => [ 'status-list' ]}
  - status_list.each do |status|
    %option{ :value => status.id , :selected => item.status.shortname.upcase }
      #{ status.description }