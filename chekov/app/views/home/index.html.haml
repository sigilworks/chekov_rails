
= render :partial => "partials/headermenu"

%div#gradient

= render :partial => "partials/flashmessages", :locals => { :flash => flash }

%div#app-wrapper
  %header
    %h1
      *
      %span.app-name>=app_version
    = render :partial => "partials/statusfilterselect", :locals => { :status_list => @status_list }

    %span#new-task-button
      %button#new-task-init.chekov-button
        %span.plus +
        new task
    = render :partial => "partials/taskstats", :locals => { :stats => stats }

  %div.main

    %table#task-table{ :role => 'main' }
      %thead
        %tr.header-row
          %th.id-header id
          %th app
          %th build
          %th description
          %th browser
          %th bz id
          %th reporter

      - task_list.each do |task|
        = render :partial => "partials/taskrowview", :locals => { :task => task }

  -#= render :partial => "partials/edittask", :locals => { :edit_view => edit_task(1) }
  -#= render :partial => "partials/newcommentview", :locals => { :comment => comment_for_task(8) }
  -#= render :partial => "partials/newtaskview", :locals => { :task => new_task }